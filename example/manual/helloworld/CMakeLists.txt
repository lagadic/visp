PROJECT(HelloWorld)

FIND_PACKAGE(VISP REQUIRED)
IF(VISP_FOUND)
  INCLUDE(${VISP_USE_FILE})
ELSE (VISP_FOUND)
  MESSAGE(FATAL_ERROR "ViSP not found. Please set VISP_DIR.")
ENDIF(VISP_FOUND)

ADD_EXECUTABLE(HelloWorld HelloWorld.cpp)
