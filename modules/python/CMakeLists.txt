# cmake_minimum_required (VERSION 3.5)
# project(visp)

# find_package(pybind11 REQUIRED)
# find_package(VISP 3.5.1 REQUIRED)
# include_directories(${VISP_INCLUDE_DIRS})
# include_directories(include)
# message(${VISP_DIR})
# string(REPLACE ";"  "\n" str "${VISP_INCLUDE_DIRS}")
# message(STATUS ${str})
# string(REPLACE ";"  "\n" str "${VISP_LIBRARIES}")
# message(STATUS ${str})
# message(${VISP_VERSION})
# message(${pybind11_VERSION})
# file(GLOB generated_cpp src/generated/*.cpp)
# file(GLOB static_cpp src/static/*.cpp)

# pybind11_add_module(visp src/main.cpp ${generated_cpp} ${static_cpp} THIN_LTO)
# target_link_libraries(visp PRIVATE ${VISP_LIBRARIES})
