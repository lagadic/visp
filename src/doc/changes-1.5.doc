/*! \page changes-1.5.html

<h1 align="center">
Change notes for ViSP 1.5
</h1><br clear="all">

\latexonly
\label{changes-1.5}
\endlatexonly

<br>

This file describes the changes between ViSP 1.4 and 1.5.

<h3>General</h3>

Sorry comments are in french

<ul>

<li> Ajout de fonctions calculant la pseudo inverse dans la classe CMatrix
   (Calcule la pseudo inverse Jp de la matrice courante et renvoie son rang)
<li> Ajout dans la classe CMatrix d'un operator double*CMatrix
<li> Ajout d'un fichier simple-math.h dans math.  Definition dans ce fichiers des fonctions :
<ul> 
<li>Fact(n) calcul de n!
<li>Comb(nb) : calcul de combinaisons
<li>Exp(x,ex) : calcul de puissance (pow(x,ex))
</ul>
<li> Ajout de fonctions de calcul des moments d'ordre 2 dans la classe
  CDot et CBlack Dot
<li> Possibilite de recuperer ces informations depuis la classe CPointDot
  (avec calcul en metre de ces informations)
<li> Creation d'une classe CCircleDot
<li> Ajout de la classe CBlackDot pour le suivi de points noir sur fond blanc
<li> Modification de la classe CPointDot pour suivre ces points
<li> Ajout d'une classe CCircleSimu derive de la classe CCircle
<li> CCylinder.h et CCylinder.c
    Ajout d'une fonction CCylinder::Display(CImage, CCamera, int col) ;
<li> CLine.h et CLine.c
    Ajout d'une fonction CCylinder::Display(CImage, CCamera, int col) ;
<li>  angle.h  angle.cpp
    Ajout de void RToUTheta(CMatrixHomogeneous& M,CColVector& T)  ;
    Ajout de void UThetaToR(CColVector& T, CMatrixHomogeneous& M) ;
<li> classe  CLineSimu :  Ajout de l'operateur =
<li> Ajout dans CMatrix d'une fonction qui teste 
  si une matrice est une matrice de rotation :
<li> Ajout dans CMatrixHomogeneous d'une fonction qui teste 
  si la matrice est bien une matrice homogene (en testant 
  la matrice de rotation) 
<li>
 Suppression de l'ajout automatique du suffixe .pgm 
  dans les fonctions ReadPGM et WritePGM
<li>
 Ajout d'une fonction:     CreateColorImage(CImage<CColor) &Ic, 
                        CImage<unsigned char> &I) ;
  dans le fichier image/UtilsImage.cpp
  Copie une image en niveau de gris dans une image couleur.
<li> Ajout d'un fichier include/Default-Setting.h contenant des parametres par
  default 
  Ce fichier contient entre autre les parametres de calibration 
  par defaut des cameras

</ul>


*/